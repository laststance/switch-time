import { ACTIVITY_PALETTE } from '@switch-time/shared'

// Placeholder entry: MVP-06 (#7) replaces this package with the Expo SDK 57 app
// (delete this file when the template is extracted). Imports shared so the wiring is type-checked.
export const paletteSize = ACTIVITY_PALETTE.length

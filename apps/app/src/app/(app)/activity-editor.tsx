import { Sheet } from '@/components/sheet'

// Content lands with MVP-17.
export default function ActivityEditorSheet() {
  return (
    <Sheet
      title="活動項目"
      hint="タップで色・アイコンを変更、★は起動時の状態"
    />
  )
}
